# Default values for public-six-degrees.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
service:
  name: "" # The name of the service, should be defined in the specific app-configs folder.
  hasHealthcheck: "true"
replicaCount: 2
image:
  repository: coco/public-six-degrees
  pullPolicy: IfNotPresent
env:
  graphitePrefix: coco.services.k8s.public-six-degrees-api
resources:
  requests:
    memory: 32Mi
  limits:
    memory: 128Mi
